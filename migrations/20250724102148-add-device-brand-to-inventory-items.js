'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  up: async (queryInterface, Sequelize) => {
    // No operation: deviceBrand column already exists
  },
  down: async (queryInterface, Sequelize) => {
    // No operation: deviceBrand column already exists
  }
};
