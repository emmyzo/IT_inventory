<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Olam Agri IT Inventory - Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      background: radial-gradient(circle at 20% 30%, #ffa726 30%, transparent 60%),
                  radial-gradient(circle at 80% 70%, #8bc34a 30%, transparent 60%),
                  linear-gradient(120deg, #ffa726 0%, #8bc34a 100%);
      background-repeat: no-repeat;
      background-attachment: fixed;
      position: relative;
    }
    .circle {
      position: absolute;
      border-radius: 50%;
      opacity: 0.3;
      z-index: 0;
    }
    .circle1 { width: 400px; height: 400px; left: -100px; top: -100px; background: #ffa726; }
    .circle2 { width: 200px; height: 200px; right: 10%; top: 20%; background: #8bc34a; }
    .circle3 { width: 150px; height: 150px; left: 20%; bottom: 10%; background: #ffd54f; }
    .login-container {
      position: relative;
      z-index: 1;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-card {
      background: #f5f5dc;
      border-radius: 18px;
      box-shadow: 0 4px 32px rgba(0,0,0,0.08);
      padding: 0;
      max-width: 595px; /* 700px - 15% */
      width: 100%;
      display: flex;
      flex-direction: row;
      overflow: hidden;
    }
    .login-left {
      background: #fffbe7;
      flex: 1 1 45%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2.5rem 2rem 2rem 2rem;
      border-right: 1px solid #eee;
      position: relative;
    }
    .logo {
      width: 50px;
      height: 30px;
      margin-bottom: 0.5rem;
      position: absolute;
      top: 1.2rem;
      left: 2rem;
    }
    .brand-title, .brand-sub {
      width: 100%;
      text-align: center;
    }
    .brand-title {
      font-size: 2.8rem;
      font-weight: 800;
      color: #222;
      margin-top: 2.5rem;
      margin-bottom: 0.2rem;
      letter-spacing: -1px;
      width: 100%;
      line-height: 1.1;
    }
    .brand-sub {
      font-size: 1.7rem;
      font-weight: 700;
      color: #009688;
      margin-bottom: 1.2rem;
      width: 100%;
    }
    .login-right {
      flex: 1 1 55%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 2.5rem 2rem;
    }
    .welcome {
      font-size: 2rem;
      font-weight: 800;
      color: #222;
      margin-bottom: 1.2rem;
      text-align: left;
    }
    .form-label {
      font-weight: 600;
      color: #333;
    }
    .btn-login {
      background: #fff;
      color: #009688;
      border: 1.5px solid #009688;
      font-weight: 600;
      border-radius: 6px;
      transition: background 0.2s, color 0.2s;
      width: 40%;
      font-size: 0.9rem;
      display: block;
      margin: 0 auto;
    }
    .btn-login:hover {
      background: #009688;
      color: #fff;
    }
    @media (max-width: 900px) {
      .login-card { flex-direction: column; max-width: 95vw; }
      .login-left, .login-right { padding: 1.5rem 1rem; border-right: none; }
      .btn-login { width: 60%; }
      .brand-title { font-size: 2rem; }
      .brand-sub { font-size: 1.2rem; }
      .welcome { font-size: 1.3rem; }
    }
    @media (max-width: 600px) {
      .login-card { padding: 0.5rem; }
      .brand-title { font-size: 1.2rem; }
      .brand-sub { font-size: 0.9rem; }
      .welcome { font-size: 1.1rem; }
      .btn-login { width: 100%; }
    }
  </style>
</head>
<body>
  <div class="circle circle1"></div>
  <div class="circle circle2"></div>
  <div class="circle circle3"></div>
  <div class="login-container">
    <div class="login-card">
      <div class="login-left">
        <img src="/images/olam_logo.png" alt="Olam Agri Logo" class="logo">
        <div class="brand-title">Olam Agri</div>
        <div class="brand-sub">IT Inventory</div>
      </div>
      <div class="login-right">
        <div class="welcome">Welcome Back!</div>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger py-2" role="alert"><%= error %></div>
        <% } %>
        <form method="POST" action="/login">
          <div class="mb-3">
            <label for="email" class="form-label">Email ID</label>
            <input type="email" class="form-control" id="email" name="email" required autocomplete="username">
          </div>
          <div class="mb-4">
            <label for="password" class="form-label">Password</label>
            <input type="password" class="form-control" id="password" name="password" required autocomplete="current-password">
          </div>
          <button type="submit" class="btn btn-login">Login</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html> 